This folder is the **NetCast** web UI (Next.js). Overview and Vercel notes are in the [repository README](../README.md).

```bash
npm install
npm run dev
```

Copy `.env.local.example` to `.env.local` when you want to set API keys.

Open [http://localhost:3000](http://localhost:3000).
