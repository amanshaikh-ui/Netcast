{
  "name": "link-search",
  "private": true,
  "description": "Run UI from repo root: npm run dev",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "start": "npm --prefix frontend run start"
  }
}
